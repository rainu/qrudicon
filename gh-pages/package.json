{
  "name": "qrudicon",
  "version": "1.0.0",
  "description": "A combination of QR-Code and Identicon",
  "author": "rainu",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "DEPLOY_ENV=GH_PAGES nuxt build",
    "start": "nuxt start",
    "generate": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup --allow-unclean"
  },
  "dependencies": {
    "@nuxtjs/pwa": "^2.6.0",
    "bootstrap": "^4.1.3",
    "bootstrap-vue": "^2.0.0-rc.11",
    "nuxt": "^1.0.0",
    "vuelidate": "^0.7.4"
  },
  "devDependencies": {
    "cross-env": "^5.0.1",
    "push-dir": "^0.4.1"
  }
}
